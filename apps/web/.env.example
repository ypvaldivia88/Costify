# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# MONGODB_URI: MongoDB connection string for cloud sync (server-side only).
MONGODB_URI="mongodb+srv://USER:PASSWORD@cluster.example.net/costify?retryWrites=true&w=majority"

# AUTH_SECRET: Secret for signing session cookies (min. 32 chars).
AUTH_SECRET="replace-with-a-long-random-secret"

# Super admin bootstrap (sincronizado en cada login si cambian las variables).
SUPER_ADMIN_EMAIL="admin@costify.local"
SUPER_ADMIN_PASSWORD="CostifyAdmin2026!"
SUPER_ADMIN_NAME="Super Admin"

# ELTOQUE_API_TOKEN: Token de acceso a la API TRMI de elTOQUE (solo servidor).
ELTOQUE_API_TOKEN=""
