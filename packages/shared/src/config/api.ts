/**
 * URL pública del backend Costify.
 * La API REST vive en `apps/web` (Next.js App Router → `app/api/*`).
 * En producción se despliega en Vercel; la app móvil consume estos endpoints.
 */
export const COSTIFY_API_BASE_URL = 'https://costify-iota.vercel.app';
